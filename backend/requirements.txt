# ============================================================
# requirements.txt —— 后端依赖（最小集，均已在本机安装）
# 安装：pip install -r requirements.txt
# ============================================================
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
httpx>=0.27,<1.0
PyYAML>=6.0,<7.0